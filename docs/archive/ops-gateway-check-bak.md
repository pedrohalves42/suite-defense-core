# Archived: supabase/functions/ops-gateway/legacy/check.ts.bak

Removed in Bloco C (2026-06-24) as part of repo cleanup.

The original file was a `.bak` snapshot of the legacy `ops-gateway/check` handler.
Active code lives under `supabase/functions/ops-gateway/`. If history is needed,
recover from git via `git log --all -- supabase/functions/ops-gateway/legacy/check.ts.bak`.

Anti-regression: `*.bak` is now gitignored and CI-blocked.
